# دليل البدء السريع — أثر طيب

## التثبيت في 5 دقائق ⚡

### الخطوة 1: استيراد قاعدة البيانات (دقيقة واحدة)

```bash
mysql -u root -p < sql/athartayeb_schema.sql
```

أو عبر phpMyAdmin:
1. أنشئ قاعدة بيانات: `athartayeb_db`
2. استورد ملف: `sql/athartayeb_schema.sql`

### الخطوة 2: تحديث الإعدادات (دقيقة واحدة)

افتح `includes/config.php` وعدّل:

```php
define('DB_HOST', 'localhost');
define('DB_NAME', 'athartayeb_db');
define('DB_USER', 'root');
define('DB_PASS', '');  // كلمة مرور MySQL

define('BASE_URL', 'http://localhost/athar-tayeb/public');
define('ADMIN_URL', 'http://localhost/athar-tayeb/admin');
```

### الخطوة 3: إنشاء مجلدات الرفع (30 ثانية)

**Windows:**
- أنشئ مجلد: `public/uploads/memorials`
- أنشئ مجلد: `cache`
- أنشئ مجلد: `logs`

**Linux/Mac:**
```bash
mkdir -p public/uploads/memorials cache logs
chmod 755 public/uploads/memorials cache logs
```

### الخطوة 4: تفعيل mod_rewrite (30 ثانية)

**XAMPP/AMPPS:**
- عادة مفعّل افتراضياً
- إذا لم يعمل، راجع `httpd.conf`

### الخطوة 5: اختبار الموقع (دقيقة واحدة)

1. افتح: `http://localhost/athar-tayeb/public/`
2. يجب أن تظهر الصفحة الرئيسية ✅

## تسجيل الدخول للإدارة

- **الرابط:** `http://localhost/athar-tayeb/admin/login.php`
- **المستخدم:** `admin`
- **كلمة المرور:** `admin123`

⚠️ **غيّر كلمة المرور فوراً!**

## اختبار الميزات

### 1. إنشاء صفحة تذكارية تجريبية

1. اذهب للصفحة الرئيسية
2. اضغط "أنشئ صفحة تذكارية"
3. املأ البيانات:
   - الاسم: محمد أحمد
   - النوع: ذكر
   - (اختياري) ارفع صورة
4. اضغط "إنشاء"
5. ستظهر صفحة النجاح مع الرابط

### 2. مراجعة الصورة (الإدارة)

1. سجّل دخول للإدارة
2. اذهب إلى "الصور" من القائمة
3. ستجد الصورة قيد المراجعة
4. اضغط "موافقة"
5. الآن الصورة ستظهر في الصفحة العامة

### 3. نشر الصفحة

1. في لوحة التحكم، اذهب إلى "الصفحات التذكارية"
2. ابحث عن الصفحة التي أنشأتها
3. اضغط "نشر"
4. الآن الصفحة ستظهر في الصفحة الرئيسية

### 4. اختبار التسبيح

1. افتح الصفحة التذكارية
2. اضغط على أي عداد تسبيح
3. يجب أن يزيد العداد فوراً
4. أعد تحميل الصفحة — العداد محفوظ ✅

### 5. اختبار البحث

1. في الصفحة الرئيسية
2. اكتب اسماً في مربع البحث
3. يجب أن تظهر نتائج مباشرة

## الصفحات الرئيسية

| الصفحة | الرابط | الوصف |
|--------|--------|-------|
| الرئيسية | `/public/` | الصفحة الرئيسية |
| إنشاء صفحة | `/public/create.php` | نموذج إنشاء صفحة جديدة |
| جميع الصفحات | `/public/all.php` | قائمة بجميع الصفحات |
| البحث | `/public/search.php` | صفحة البحث |
| لوحة التحكم | `/admin/dashboard.php` | لوحة تحكم الإدارة |

## قائمة التحقق السريع ✅

- [ ] قاعدة البيانات تم إنشاؤها واستيرادها
- [ ] ملف `config.php` تم تحديثه
- [ ] مجلدات الرفع تم إنشاؤها
- [ ] الصفحة الرئيسية تعمل
- [ ] يمكن إنشاء صفحة تذكارية
- [ ] تسجيل الدخول للإدارة يعمل
- [ ] الصور تُرفع بنجاح
- [ ] التسبيح يعمل ويُحفظ
- [ ] البحث يعمل
- [ ] تم تغيير كلمة مرور الإدارة

## المشاكل الشائعة وحلولها

### المشكلة: "Database connection failed"
**الحل:** تحقق من بيانات الاتصال في `config.php`

### المشكلة: الصفحة بيضاء فارغة
**الحل:** فعّل عرض الأخطاء:
```php
// في config.php
define('ENV', 'development');
```

### المشكلة: الصور لا تُرفع
**الحل:** 
1. تأكد من وجود مجلد `public/uploads/memorials`
2. تأكد من أذونات الكتابة (755)

### المشكلة: 404 للصفحات التذكارية
**الحل:** تأكد من تفعيل mod_rewrite

### المشكلة: التسبيح لا يُحفظ
**الحل:** تحقق من:
1. الاتصال بقاعدة البيانات
2. CSRF token موجود في الصفحة
3. JavaScript يعمل (افتح Console)

## الخطوات التالية

1. ✅ غيّر كلمة مرور الإدارة
2. ✅ عدّل معلومات الموقع في `config.php`
3. ✅ خصص الألوان في `main.css`
4. ✅ أضف معلومات التواصل في `contact.php`
5. ✅ فعّل HTTPS إن أمكن
6. ✅ اختبر جميع الميزات
7. ✅ أنشئ نسخة احتياطية

## البيانات التجريبية

قاعدة البيانات تحتوي على 3 صفحات تجريبية:

1. **محمد أحمد السيد** (منشور)
2. **فاطمة محمود علي** (منشور)
3. **عبدالله خالد** (قيد المراجعة)

يمكنك حذفها من لوحة التحكم بعد الاختبار.

## الأوامر المفيدة

### نسخ احتياطي سريع:
```bash
mysqldump -u root -p athartayeb_db > backup.sql
```

### استعادة النسخة الاحتياطية:
```bash
mysql -u root -p athartayeb_db < backup.sql
```

### مسح الكاش:
```bash
rm -rf cache/*
```

## الدعم السريع

- 📧 البريد: support@athartayeb.com
- 📱 WhatsApp: +20 123 456 7890
- 📖 الدليل الكامل: [INSTALL.md](INSTALL.md)

---

**تهانينا! 🎉**

موقعك جاهز الآن. ابدأ بإنشاء صفحات تذكارية ومشاركة الخير.

© 2025 أثر طيب — صدقة جارية رقمية
